package main

import "fmt"

func main() {
	fmt.Println("What are resource paths?")
	// A server usually has lots of different resource it can send to a browser.
	// A resource path tells the server which of its many resources we want to act on.
}
