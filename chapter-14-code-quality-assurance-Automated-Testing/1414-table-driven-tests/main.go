package main

import "fmt"

func main() {
	// There is a bit of duplicated code between the tests.
	fmt.Println("What are the the only things that vary between tests?")
	// The INPUT we passed and the OUTPUT we expect.
}
